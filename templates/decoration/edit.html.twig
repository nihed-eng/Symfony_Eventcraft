{% extends 'base.html.twig' %}

{% block title %}Modifier la décoration{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1>Modifier la décoration</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-success mt-3">Enregistrer</button>
    {{ form_end(form) }}
</div>
{% endblock %}
