{% extends 'base.html.twig' %}

{% block title %}Réinitialisation du mot de passe (DEV MODE){% endblock %}

{% block body %}
<div class="auth-container">
    <div class="auth-card" style="max-width: 600px; margin: 2rem auto; padding: 2rem; background-color: #f8f9fa; border-radius: 0.5rem; box-shadow: 0 0.5rem 1rem rgba(0,0,0,0.15);">
        <div style="background-color: #fff3cd; color: #856404; padding: 1rem; margin-bottom: 1.5rem; border-radius: 0.375rem; border: 1px solid #ffeeba;">
            <h4 style="margin-top: 0;"><strong>ENVIRONNEMENT DE DÉVELOPPEMENT UNIQUEMENT</strong></h4>
            <p>Cette page n'est accessible qu'en mode développement et permet de contourner l'envoi d'emails.</p>
        </div>
        
        <h1>Lien de réinitialisation de mot de passe</h1>
        <p>
            Voici le lien de réinitialisation de mot de passe qui aurait normalement été envoyé par email :
        </p>
        
        <div style="background-color: #e9ecef; padding: 1rem; border-radius: 0.375rem; margin: 1.5rem 0; word-break: break-all;">
            <a href="{{ resetUrl }}" class="btn btn-primary" style="display: inline-block; background-color: #007bff; color: white; padding: 0.5rem 1rem; text-decoration: none; border-radius: 0.25rem; margin-bottom: 1rem;">Réinitialiser le mot de passe</a>
            <br>
            <small style="display: block; margin-top: 0.5rem;">URL: <code>{{ resetUrl }}</code></small>
            <small style="display: block; margin-top: 0.5rem;">Token: <code>{{ tokenData.token }}</code></small>
        </div>
        
        <p>Ce lien expirera dans {{ tokenData.expirationMessageKey|trans(tokenData.expirationMessageData, 'ResetPasswordBundle') }}.</p>
        
        <div class="auth-actions" style="margin-top: 2rem;">
            <a href="{{ path('app_login') }}" class="btn btn-secondary" style="display: inline-block; background-color: #6c757d; color: white; padding: 0.5rem 1rem; text-decoration: none; border-radius: 0.25rem;">Retour à la connexion</a>
        </div>
    </div>
</div>
{% endblock %}