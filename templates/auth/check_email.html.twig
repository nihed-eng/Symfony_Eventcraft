{% extends 'base.html.twig' %}

{% block title %}Email de réinitialisation envoyé{% endblock %}

{% block body %}
<div class="auth-container">
    <div class="auth-card">
        <h1>Email envoyé</h1>
        <p>
            Un email contenant un lien pour réinitialiser votre mot de passe a été envoyé à l'adresse email que vous avez fournie.
            
            {% if devMode is defined and devMode %}
                Ce lien expirera dans {{ tokenData.expirationMessageKey|trans(tokenData.expirationMessageData, 'ResetPasswordBundle') }}.
            {% else %}
                Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
            {% endif %}
        </p>
        <p>Si vous n'avez pas reçu d'email, vérifiez votre dossier spam ou <a href="{{ path('app_forgot_password_request') }}">essayez à nouveau</a>.</p>
        <div class="auth-actions">
            <a href="{{ path('app_login') }}" class="btn btn-secondary">Retour à la connexion</a>
        </div>
    </div>
</div>
{% endblock %}